---
- name: Configure dev machine
  hosts: all

  pre_tasks:
    - name: Install general tools
      become: true
      apt:
        update_cache: true
        cache_valid_time: 3600
        pkg:
          - tmux

  roles:
    - role: vim
    - role: avr
